using System;

class Program
{
    static void Main(string[] args)
    {
        Console.WriteLine("Hello Develop05 World!");
        // AdvanceGoal methods currently only change _completed to true when applicable
        // No external logic has been written
        // UPDATE 7/1: When the menu tells a goal to advance, it will run both AdvanceGoal, and then run GetPoints if it returns true.
    }
}